package apack;

public class A {
    public int pub = 1;        // Accessible everywhere
    protected int prot = 2;    // Accessible in subclasses & same package
    private int priv = 3;      // Only accessible in same class
}
